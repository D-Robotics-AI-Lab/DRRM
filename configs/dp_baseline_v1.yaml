_target_: drrm.trainer.train_dp.train
# The output directory where the model predictions and checkpoints will be written.
output_dir: checkpoints/dp_baseline_v1
# [TensorBoard](https://www.tensorflow.org/tensorboard) log directory. Will default to *output_dir/runs/**CURRENT_DATETIME_HOSTNAME***.
logging_dir: logs
# A seed for reproducible training.
seed: 42
# Whether or not to allow TF32 on Ampere GPUs. Can be used to speed up training. For more information, 
# see https://pytorch.org/docs/stable/notes/cuda.html#tensorfloat-32-tf32-on-ampere-devices
allow_tf32: False
# Initial learning rate (after the potential warmup period) to use.
learning_rate: 1e-4
# lerobot dataset directory
dataset_root: /root/data/projects/embodiedai/RoboticsManipulation/RoboticsManipulation/data/lerobot/dual_bottles_pick_easy_D435
# Number of epochs to sample from the dataset.
num_epochs: 300
# Batch size (per device) for the training dataloader.
train_batch_size: 128
# Batch size (per device) for the sampling dataloader.
sample_batch_size: 64
# Whether training should be resumed from a previous checkpoint. Use a path saved by
# `--checkpointing_period`, or `"latest"` to automatically select the last available checkpoint.
resume_from_checkpoint: checkpoint-10
# Save a checkpoint of the training state every X updates. Checkpoints can be used for resuming training via `--resume_from_checkpoint`. 
# In the case that the checkpoint is better than the final trained model, the checkpoint can also be used for inference.
# Using a checkpoint for inference requires separate loading of the original pipeline and the individual checkpointed model components.
# See https://huggingface.co/docs/diffusers/main/en/training/dreambooth#performing-inference-using-a-saved-checkpoint for step by step instructions.
checkpointing_period: 10
# Run sampling every X steps. During the sampling phase, the model will sample a trajectory 
# and report the error between the sampled trajectory and groud-truth trajectory in the training batch.
sample_period: 10
# Number of batches to sample from the dataset.
num_sample_batches: 2
# Save more memory by using setting grads to None instead of zero. Be aware, that this changes certain behaviors, 
# so disable this argument if it causes any problems. More info:
# https://pytorch.org/docs/stable/generated/torch.optim.Optimizer.zero_grad.html
set_grads_to_none: False
# Max gradient norm.
max_grad_norm: 1.0





accelerator:
  # Max number of checkpoints to store. Passed as `total_limit` to the `Accelerator` `ProjectConfiguration`.
  # See Accelerator::save_state https://huggingface.co/docs/accelerate/package_reference/accelerator#accelerate.Accelerator.save_state for more details
  checkpoints_total_limit: 4
  # The integration to report the results and logs to. Supported platforms are `"tensorboard"` (default), `"wandb"` and `"comet_ml"`. 
  # Use `"all"` to report to all integrations.
  report_to: "wandb"
  # Whether to use mixed precision. Choose between fp16 and bf16 (bfloat16). Bf16 requires PyTorch >= 1.10.and an Nvidia Ampere GPU.  
  # Default to the value of accelerate config of the current system or the flag passed with the `accelerate.launch` command. 
  # Use this argument to override the accelerate config.
  mixed_precision: "no" # "no", "fp16", "bf16"
  # Number of updates steps to accumulate before performing a backward/update pass.
  gradient_accumulation_steps: 1

image_shape: [3, 240, 320]
shape_meta:
  # acceptable types: rgb, low_dim
  obs:
    head_cam:
      shape: ${image_shape}
      type: rgb
    # front_cam:
    #   shape: *image_shape
    #   type: rgb
    # left_cam:
    #   shape: *image_shape
    #   type: rgb
    # right_cam:
    #   shape: *image_shape
    #   type: rgb
    agent_pos:
      shape: [14]
      type: low_dim
  action:
    shape: [14]
horizon: 8
n_obs_steps: 3
n_action_steps: 8
n_latency_steps: 0
obs_as_global_cond: True
model:
  _target_: drrm.models.policy.diffusion_policy.diffusion_unet_image_policy.DiffusionUnetImagePolicy

  shape_meta: ${shape_meta}
  
  noise_scheduler:
    _target_: diffusers.schedulers.scheduling_ddpm.DDPMScheduler
    num_train_timesteps: 100
    beta_start: 0.0001
    beta_end: 0.02
    beta_schedule: squaredcos_cap_v2
    variance_type: fixed_small # Yilun's paper uses fixed_small_log instead, but easy to cause Nan
    clip_sample: True # required when predict_epsilon=False
    prediction_type: epsilon # or sample

  obs_encoder:
    _target_: drrm.models.policy.diffusion_policy.multi_image_obs_encoder.MultiImageObsEncoder
    shape_meta: ${shape_meta}
    rgb_model:
      _target_: drrm.models.policy.diffusion_policy.vision.model_getter.get_resnet
      name: resnet18
      weights: null
    resize_shape: null
    crop_shape: null
    # constant center crop
    random_crop: True
    use_group_norm: True
    share_rgb_model: False
    imagenet_norm: True

  horizon: ${horizon}
  n_action_steps: ${eval:'${n_action_steps}+${n_latency_steps}'}
  n_obs_steps: ${n_obs_steps}
  num_inference_steps: 100
  obs_as_global_cond: ${obs_as_global_cond}
  # crop_shape: null
  diffusion_step_embed_dim: 128
  # down_dims: [512, 1024, 2048]
  down_dims: [256, 512, 1024]
  kernel_size: 5
  n_groups: 8
  cond_predict_scale: True

ema:
  _target_: drrm.models.ema_model.EMAModel
  update_after_step: 0
  inv_gamma: 1.0
  power: 0.75
  min_value: 0.0
  max_value: 0.9999

optimizer:
  _target_: torch.optim.AdamW
  lr: ${learning_rate}
  betas: [0.9, 0.999] # The beta1 and beta2 parameters for the Adam optimizer.
  weight_decay: 1e-2 # The weight decay to use.
  eps: 1e-8

train_dataset:
  _target_: drrm.datasets.drrm_dataset.DRRMDataset
  root: ${dataset_root}
  repo_id: D-robotics/dual_bottles_pick_easy_D435
  horizon: ${horizon}
  pad_before: 3
  pad_after: 3
  seed: 42
  val_ratio: 0.02
  npy_feature_keys: null
  # - spatial_tokens_23
  # - image_tokens_pos
  dataset_metadata:
    _target_: lerobot.common.datasets.lerobot_dataset.LeRobotDatasetMetadata
    root: ${dataset_root}
    repo_id: D-robotics/dual_bottles_pick_hard_D435

train_dataloader:
  batch_size: ${train_batch_size}
  drop_last: True
  num_workers: 8
val_dataloader:
  batch_size: ${sample_batch_size}
  shuffle: False
  num_workers: 2
  pin_memory: True
  persistent_workers: True

scheduler:
  # The scheduler type to use. Choose between ["linear", "cosine", "cosine_with_restarts", "polynomial", "constant", "constant_with_warmup"]
  lr_scheduler: constant
  # Number of steps for the warmup in the lr scheduler.
  lr_warmup_steps: 0.1
  # Number of hard resets of the lr in cosine_with_restarts scheduler.
  lr_num_cycles: 1
  # Power factor of the polynomial scheduler.
  lr_power: 1.0

















